<Page x:Class="TestManager.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
      xmlns:core="using:Microsoft.Xaml.Interactions.Core"
      mc:Ignorable="d">
    <Grid x:Name="ContentArea"
          BorderThickness="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <TabView SelectedIndex="0"
                 IsAddTabButtonVisible="False"
                 MinHeight="475"
                 TabWidthMode="SizeToContent">
            <TabView.TabItems>
                <TabViewItem  Header="Home"
                              IsClosable="False">
                    <TabViewItem.IconSource>
                        <SymbolIconSource Symbol="Home" />
                    </TabViewItem.IconSource>
                </TabViewItem>
                <TabViewItem  Header="Tab 2 Has Longer Text"
                              IsClosable="False">
                    <TabViewItem.IconSource>
                        <SymbolIconSource Symbol="MusicInfo" />
                    </TabViewItem.IconSource>
                </TabViewItem>
                <TabViewItem  Header="Third Tab"
                              IsClosable="False">
                    <TabViewItem.IconSource>
                        <SymbolIconSource Symbol="Placeholder" />
                    </TabViewItem.IconSource>
                </TabViewItem>
            </TabView.TabItems>
        </TabView>
        <StackPanel Spacing="8"
                    Grid.Column="1"
                    Margin="10,0,0,0"
                    x:Name="StackPanel_MainRight">
            <ComboBox Width="{x:Bind StackPanel_MainRight.ActualWidth ,Mode=OneWay}"
                      PlaceholderText="选择设备型号"
                      MinWidth="200"
                      ToolTipService.ToolTip="选择您的设备型号"
                      ItemsSource="{x:Bind ViewModel.DsModels}"
                      SelectedValue="{x:Bind ViewModel.DsModel ,Mode=TwoWay}" />
            <Expander Width="{x:Bind StackPanel_MainRight.ActualWidth ,Mode=OneWay}"
                      MinWidth="200"
                      HorizontalContentAlignment="Left"
                      IsExpanded="True">
                <Expander.Header>
                    <ToggleSwitch OffContent="串口关闭"
                                  OnContent="串口打开"
                                  Toggled="ToggleSwitch_Toggled"></ToggleSwitch>
                </Expander.Header>
                <Expander.Content>
                    <StackPanel Orientation="Vertical"
                                Spacing="15">
                        <ComboBox PlaceholderText="选择端口号"
                                  ItemsSource="{x:Bind ViewModel.Ports}"
                                  MinWidth="170"
                                  SelectedIndex="0"
                                  ToolTipService.ToolTip="设置通信端口。"
                                  SelectedItem="{x:Bind ViewModel.Port ,Mode=TwoWay}"
                                  Header="端口号"
                                  IsEnabled="{x:Bind ViewModel.DisableSerialPortEdit,Mode=OneWay}"/>
                        <ComboBox PlaceholderText="设置波特率"
                                  ItemsSource="{x:Bind ViewModel.BaudRates}"
                                  MinWidth="170"
                                  IsEditable="False"
                                  SelectedIndex="2"
                                  ToolTipService.ToolTip="设置串行波特率。"
                                  SelectedValue="{x:Bind ViewModel.BaudRate ,Mode=TwoWay}"
                                  Header="波特率"
                                  IsEnabled="{x:Bind ViewModel.DisableSerialPortEdit,Mode=OneWay}" />
                        <ComboBox PlaceholderText="选择校验位"
                                  ItemsSource="{x:Bind ViewModel.Paritys}"
                                  MinWidth="170"
                                  SelectedIndex="0"
                                  ToolTipService.ToolTip="指定 SerialPort 对象的奇偶校验位。"
                                  Header="校验位"
                                  IsEnabled="{x:Bind ViewModel.DisableSerialPortEdit,Mode=OneWay}" />
                        <ComboBox PlaceholderText="选择数据位"
                                  ItemsSource="{x:Bind ViewModel.DataBits}"
                                  MinWidth="170"
                                  SelectedIndex="3"
                                  ToolTipService.ToolTip="设置每个字节的标准数据位长度。"
                                  Header="数据位"
                                  IsEnabled="{x:Bind ViewModel.DisableSerialPortEdit,Mode=OneWay}" />
                        <ComboBox PlaceholderText="选择停止位"
                                  ItemsSource="{x:Bind ViewModel.StopBits}"
                                  MinWidth="170"
                                  SelectedIndex="1"
                                  ToolTipService.ToolTip="指定在 SerialPort 对象上使用的停止位的数目。"
                                  Header="停止位"
                                  IsEnabled="{x:Bind ViewModel.DisableSerialPortEdit,Mode=OneWay}" />
                        <ComboBox PlaceholderText="选择控制协议"
                                  ItemsSource="{x:Bind ViewModel.HandShakes}"
                                  MinWidth="170"
                                  IsEnabled="{x:Bind ViewModel.DisableSerialPortEdit,Mode=OneWay}"
                                  ToolTipService.ToolTip="指定在为 SerialPort 对象建立串行端口通信时使用的控制协议。"
                                  Header="流控制" />
                    </StackPanel>
                </Expander.Content>
            </Expander>
        </StackPanel>
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Grid.ColumnSpan="2">
            <InfoBar IsOpen="True"
                     Title="IsSuccess"
                     Message="81 55 00 00"
                     Severity="Success"
                     IsClosable="False">
                <InfoBar.ActionButton>
                    <Button Content="保存" />
                </InfoBar.ActionButton>
            </InfoBar>
        </StackPanel>
    </Grid>
</Page>
